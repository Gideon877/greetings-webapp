<title>Admin Settings</title>
</head>

<body>
    <div class="ui middle aligned center aligned container">
        <hr>
        <div class="ui piled segment">
            <h4>Settings</h4>
            <div class="ui container">
                <div class="ui labeled icon menu" id="you can remove, compact, labeled">
                    <div class="item">
                        <i class="home icon"></i>
                        <a href="/">Home</a>
                    </div>
                    <div class="item">
                        <i class="users icon"></i>
                        <a href="#">Names</a>
                    </div>
                    <div class="item">
                       <i class="language icon"></i>
                        <a href="#">Languages</a>
                    </div>
                    <div class="right item">
                        <i class="cogs icon"></i>
                        <a href="/admin">Admin</a>
                    </div>
                    <div class="item">
                        <i class="sign out icon"></i>
                        <a href="/logout">Sign-Out</a>
                    </div>
                </div>
            </div>
        </div>
        <div class="ui tall stacked segment">
            <p>settings page need lot of planning.</p>
            <div class="ui container">
                
                <div class="ui icon message">
                    <i class="inbox icon"></i>
                    <div class="content">
                        <div class="header">
                        Names greeted list full? 
                        </div>
                        <p>Clear all names from database</p>
                        {{#if messages.error}}
                             <div class="sub teal header">{{messages.error}}</div>
                        {{/if}}
                        {{#if messages.success}}
                             <div class="sub teal header">{{messages.success}}</div>
                        {{/if}}
                        {{!-- <br> <hr> --}}
                        <a href="/#" class="ui large purple button" type="cancel">Clear</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>


